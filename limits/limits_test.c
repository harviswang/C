#include <limits.h>
#include <stdio.h>

int main(int argc, char *argv[])
{
	printf("INT_MAX = %d\n", INT_MAX);
	printf("INT_MIN = %d\n", INT_MIN);
	printf("INT_MAX + INT_MIN = %d\n", INT_MAX + INT_MIN);
	return 0;
}
